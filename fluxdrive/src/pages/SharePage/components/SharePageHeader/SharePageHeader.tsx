import type { FolderPathType } from "../../../../types/folder-types";

function SharePageHeader({ folderNav }: { folderNav: FolderPathType[] }) {
  console.log(folderNav);
  return <div>SharePageHeader</div>;
}

export default SharePageHeader;
